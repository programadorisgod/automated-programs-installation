- name: Add opencode to PATH in zsh
  ansible.builtin.lineinfile:
    path: "/home/{{ ansible_user }}/.zshrc"
    line: 'export PATH="$HOME/.opencode/bin:$PATH"'
    create: true
    state: present
  become: false
